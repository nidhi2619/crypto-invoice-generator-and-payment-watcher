{
    "name": "invoice-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for crypto invoice generator",
    "scripts": {
        "test": "hardhat test",
        "deploy:qubetics": "hardhat run scripts/deploy.js --network qubetics"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "@openzeppelin/contracts": "^5.0.0",
        "hardhat": "^2.19.0",
        "dotenv": "^16.3.1"
    }
}